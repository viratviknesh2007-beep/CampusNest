"            {/* Room Detail Modal popup */}\n            {selectedRoom && (\n              <div className=\"fixed inset-0 bg-black/75 backdrop-blur-sm z-50 flex items-center justify-center p-4\">\n                <div className=\"w-full max-w-2xl glass-panel p-8 border border-white/15 rounded-3xl relative shadow-2xl animate-in fade-in zoom-in duration-200\">\n                  <button onClick={() => setSelectedRoom(null)} className=\"absolute top-5 right-5 text-slate-400 hover:text-white font-black text-lg\">✕</button>\n                  \n                  <div className=\"flex items-center gap-3 mb-2\">\n                    <h3 className=\"text-2xl font-extrabold text-white\">{selectedRoom.block_name} - Room {selectedRoom.room_number}</h3>\n                    <span className={`px-2.5 py-0.5 rounded text-[10px] font-bold uppercase ${\n                      selectedRoom.complaints_count > 0 ? \"bg-rose-500/20 text-rose-400 animate-pulse\" : \"bg-emerald-500/20 text-emerald-400\"\n                    }`}>\n                      {selectedRoom.complaints_count > 0 ? \"Active Complaints\" : \"No Issues\"}\n                    </span>\n                  </div>\n                  <p className=\"text-sm opacity-65 mb-6\">Floor {selectedRoom.floor} | Capacity: {selectedRoom.capacity} Beds</p>\n                  \n                  <div className=\"space-y-6 border-t border-white/10 pt-6\">\n                    {/* Roommates / Occupants section */}\n                    <div>\n                      <h4 className=\"text-sm font-bold uppercase tracking-wider text-indigo-400 mb-3\">Roommates</h4>\n                      {selectedRoom.occupants.length > 0 ? (\n                        <div className=\"grid grid-cols-2 gap-4\">\n                          {selectedRoom.occupants.map((o: any, idx: number) => (\n                            <div key={idx} className=\"p-4 rounded-2xl bg-white/5 border border-white/10 text-xs\">\n                              <p className=\"font-extrabold text-white text-sm\">{o.full_name}</p>\n   
<truncated 5307 bytes>