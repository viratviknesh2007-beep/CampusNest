"        {activeTab === \"heatmap\" && (\n          <div className=\"space-y-6\">\n            {/* Floor Complaint Box Visual Explorer */}\n            <div className=\"glass-panel rounded-3xl p-6 border border-white/5\">\n              <div className=\"flex justify-between items-center mb-6\">\n                <div>\n                  <h2 className=\"text-lg font-bold text-white\">Hostel Floor-Wise Complaint Box</h2>\n                  <p className=\"text-xs opacity-60\">Floors highlighted in RED contain active issues. Click a floor to view its rooms, and click any room to inspect roommates and problems.</p>\n                </div>\n                <div className=\"flex gap-4 text-xs font-semibold\">\n                  <div className=\"flex items-center gap-1.5\"><span className=\"h-3 w-3 rounded-full bg-emerald-500 block\"></span> Normal</div>\n                  <div className=\"flex items-center gap-1.5\"><span className=\"h-3 w-3 rounded-full bg-rose-500 block animate-pulse\"></span> Active Issue(s)</div>\n                </div>\n              </div>\n\n              <div className=\"space-y-6\">\n                {hostelMap.map((block, bIdx) => (\n                  <div key={bIdx} className=\"space-y-4\">\n                    <h3 className=\"text-sm font-extrabold uppercase tracking-wide text-indigo-400 border-b border-white/5 pb-2\">\n                      {block.block_name}\n                    </h3>\n                    \n                    <div className=\"grid gap-3\">\n                      {Array.from({ length: block.total_floors }, (_, i) => block.total_floors - i).map((floor) => {\n                        const floorId = `${block.block_name}-${floor}`;\n                        const floorRooms = block.rooms.filter((r: any) => r.floor === floor);\n                        const floorComplaintsCount = floorRooms.reduce((acc: number, r: any) => acc + (r.complaints_count || 0), 0);\n                        const isExpanded = expandedFloor === floorId;\n\n                        return (\n                
<truncated 11028 bytes>